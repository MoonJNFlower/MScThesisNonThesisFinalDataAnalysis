# M.Sc Research Analytics Streamlit App

This dashboard presents the thesis/non-thesis dataset with interactive filters, charts, summary metrics, detailed tables, and data-quality notes.

## Run locally

```powershell
pip install -r requirements.txt
streamlit run app.py
```

The app reads `M.Sc_thesis_non-thesis_final_data_sheet.xlsx` from this project folder.

